{
  "name": "groovelab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GrooveLab — a beautiful, browser-based simplified DAW. Vibe-coded with Vite + TypeScript + Tailwind + Tone.js.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tone": "^15.0.4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
